// Generated by CoffeeScript 1.9.0
(function() {
  var dataString, exports, http, makeRequest, onResponse, options, stayAlive;

  http = require('http');

  options = {
    host: 'badslackbot.herokuapp.com'
  };

  dataString = '';

  makeRequest = function() {
    dataString = '';
    return http.request(options, onResponse).end();
  };

  onResponse = function(response) {
    var status;
    status = response.statusCode === 200 ? "alive" : "dead";
    return console.log("The server is " + status);
  };

  stayAlive = function() {
    return setInterval(makeRequest, 1800000);
  };

  exports = this;

  exports.stayAlive = stayAlive;

}).call(this);
